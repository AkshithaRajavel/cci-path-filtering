version: '2.1'
jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: git diff --name-only HEAD HEAD~1 | xargs -I % dirname % | xargs make

workflows:
  filter:
    jobs:
      - build