version: '2.1'
jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: |
          changed_dirs=$(git diff --name-only HEAD HEAD~1 | xargs -I % dirname % )
          echo $changed_dirs

workflows:
  filter:
    jobs:
      - build