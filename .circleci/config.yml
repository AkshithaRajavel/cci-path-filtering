version: '2.1'
jobs:
  filter:
    docker:
      - image: cimg/base:2023.02
    steps:
      - checkout
      - run:
          command: echo $TAGANCH
          environment:
            TAGANCH: "v${CIRCLE_SHA1:0:7}"

      

workflows:
  main:
    jobs:
      - filter